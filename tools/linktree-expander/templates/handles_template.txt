# One Linktree handle per line. Lines starting with # are ignored.
# Handle = the slug after linktr.ee/  (NOT the full URL, although URLs are also accepted)
# Examples:
#   modibodi
#   https://linktr.ee/modibodi    ← also works
#   meg.boggs                      ← dots are valid

# === Period swimwear KOL pool seed (extracted from kol_prospects_social.csv 2026-05-06) ===
2girls1brainpodcast
Avachanel
Dominokirke
DownDogFitnessAC
Hannahshaller
